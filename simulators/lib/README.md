# Simulator libraries

This directory contains shared support for deterministic simulator execution. `virtual-clock.cjs` provides a virtual clock without real-time waits, and `statistics.cjs` summarizes numeric samples with mean, median, and nearest-rank p95 values.
